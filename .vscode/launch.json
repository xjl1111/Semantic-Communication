{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 在当前终端运行",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${fileDirname}",
            "python": "${workspaceFolder}\\.venv\\Scripts\\python.exe"
        }
    ]
}
