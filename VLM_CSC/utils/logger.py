"""Structured logger setup and metric writer utilities."""

from pathlib import Path


def setup_logger(log_dir: Path, run_name: str) -> object:
    """Create logger and file handlers."""
    raise NotImplementedError
